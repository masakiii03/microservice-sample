spring:
  application:
    name: client-1
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  instance:
    metadata-map:
      version: 1
      # version: 2

server:
  port: 8001
  # port: 8011